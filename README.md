# Colony Combo

## Development

This project must be run with Vite during development.

Use:

```bash
npm run dev
```

Go Live / Live Server will not process Vue SFCs or TypeScript imports in `src/`, so it can show a blank page.

For a static preview of the built app:

```bash
npm run build
npm run preview
```
